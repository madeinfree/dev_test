<%= link_to('文章', posts_url) %> |
<%= link_to('產品', products_url) %>
<div class="container">
<table class="table table-bordered">
  <tr>
    <td>產品名稱</td>
    <td>價格</td>
    <td>圖片</td>
    <td>動作</td>
  </tr>
  <tr>
    <td><%= @product.name %></td>
    <td><%= @product.price %></td>
    <td><%= image_tag @product.image_url(:thumb), :class => 'img-thumbnail' %></td>
    <td><%= link_to('加入購物車','', :class => "btn btn-default") %></td>
  </tr>
</table>
</div>